---
title: "pragBat model"
output: html_document
---

```{r setup, include=FALSE}
library(tidyverse)
library(tidyboot)
library(jsonlite)
library(coda)
library(ggthemes)
library(RColorBrewer)
library(ggpubr)
library(data.table)

estimate_mode <- function(s) {
  d <- density(s)
  return(d$x[which.max(d$y)])
}


hdi_upper<- function(s){
  m <- HPDinterval(mcmc(s))
  return(m["var1","upper"])
}

hdi_lower<- function(s){
  m <- HPDinterval(mcmc(s))
  return(m["var1","lower"])
}


```

# Model 

## model output
```{r}

a <-fread("../model/output/model_pragBat3-chain1.csv")
b <-fread("../model/output/model_pragBat3-chain2.csv")
#c <-fread("../model/output/model_pragBat3-chain3.csv")
d <-fread("../model/output/model_pragBat3-chain4.csv")
#e <-fread("../model/output/model_pragBat3-chain5.csv")
f <-fread("../model/output/model_pragBat3-chain6.csv")


model_raw <- bind_rows(a,b,d,f)%>%
  mutate(chain = factor(chain))

# model_params <- model%>%
#   filter(parameter != "model_predictions")
# 
#saveRDS(model_raw, "../model/output/model_params_pragBat3.rds")

#model_raw <- readRDS("../model/output/model_params_pragBat3.rds")

```

## Visualize paramters

### Semantic Knowledge

#### Global semantic knowledge

```{r}
global_sem <- model_raw %>%
  filter(parameter == "semantic_knowledge",
         type == "global_parameter")

ggplot(global_sem, aes(x = value, col = chain, fill=chain))+
  geom_density(alpha = 0.3)+
  ggtitle("Global Semantic Knowledge")+
  xlab("Value")+
  theme_minimal()+
  facet_wrap(~token, scales = "free_y")+
  scale_color_ptol()+
  scale_fill_ptol()+
  guides()

```

#### Item specific semantic knowledge

```{r}

item_params <- model_raw %>%
  filter(type == "item_parameter")

item_summary <- item_params %>%
  group_by(chain, id, token)%>%
  summarise(mode = estimate_mode(value),
            uci = hdi_upper(value),
            lci = hdi_lower(value))

sem_know_map <- item_summary%>%
  select(-uci,-lci)%>%
  spread(token, -chain) %>%
  expand_grid(., age = seq(from = 0, to = 2, by = 0.1)) %>%
  mutate(sem_know = plogis(intercept + slope * age))

ggarrange(
  
ggplot(item_params, aes(x = value, col = id, fill=id, group = chain))+
  geom_density(alpha = 0.3, aes(lty = chain))+
  xlab("")+
  theme_minimal()+
  scale_color_viridis_d()+
  scale_fill_viridis_d()+
  guides(col = F, fill = F, lty = F)+
  facet_grid(id ~token)
,

ggplot(data = sem_know_map, aes(x = age+3, y= sem_know, col = id))+
  geom_line( aes(lty = chain),size = 1)+
  ylab("Semantic knowledge")+
  xlab("Age")+
  ylim(0,1)+
  theme_minimal()+
  scale_colour_viridis_d(name = "Object")

)
```

### Speaker Optimality

#### Global speaker optimality

```{r}
global_so <- model_raw %>%
  filter(parameter == "speaker_optimality",
         type == "global_parameter",
         token != "scale_parameter")%>%
  mutate(token = factor(token, levels = c("intercept", "slope","sigma")))

ggplot(global_so, aes(x = value, col = chain, fill=chain))+
  geom_density(alpha = 0.3)+
  xlab("Value")+
  ggtitle("Global speaker optimality")+
  theme_minimal()+
  facet_grid(~token)+
  scale_color_ptol()+
  scale_fill_ptol()+
  guides()
```

#### Subject specific speaker optimality

```{r}
so_id <- model_raw %>%
  filter(parameter == "speaker_optimality",
         type == "subj_parameter",
         token == "speaker_optimality")

ggplot(so_id, aes(x = value, col = chain, fill = chain))+
  geom_density(alpha = 0.3)+
  xlab("Value")+
  ggtitle("ID specific speaker optimality")+
  theme_minimal()+
  facet_wrap(~id, scales = "free")+
  scale_color_ptol()+
  scale_fill_ptol()
```


```{r}
so_est <- model_raw %>%
  filter(parameter == "speaker_optimality",
         type == "subj_parameter",
         token == "speaker_optimality")%>%
  group_by(id)%>%
  summarise(mode = estimate_mode(value),
            uci = hdi_upper(value),
            lci = hdi_lower(value))

ggplot(so_est, aes(x = id, y = mode))+
  geom_pointrange(aes(ymin = lci, ymax = uci))+
  theme_minimal()
  

write_csv(so_est, "../data/rsa_est.csv")
```


#### Scale parameter

```{r}
scale <- model_raw %>%
  filter(parameter == "speaker_optimality",
         token == "scale_parameter")

ggplot(scale, aes(x = value, col = chain, fill=chain))+
  geom_density(alpha = 0.3)+
  xlab("Scale Parameter")+
  facet_grid(~id)+
  theme_minimal()+
  scale_color_ptol()+
  scale_fill_ptol()+
  guides()

```

